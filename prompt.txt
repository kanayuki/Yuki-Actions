我想在best目录中实现以下功能：
在github上搜索最受欢迎的一些免费v2ray节点（订阅），检测可用性，挑选出最好的100个(可配置)，保存到proxy/best.txt
日志输出到logs目录，7天滚动

best结果依然不理想，我想重新实现best.py，这次我想这样实现：
1. 从github上搜索目前活跃的最受欢迎的一些提供免费v2ray节点（订阅）的仓库，比如前10-20个，
保存到proxy/repositories.txt中
2. 将这些仓库中的订阅链接/分享链接提取出来，保存到proxy/collections.txt
3. 根据代理协议使用xray,singbox,mihomo等工具（根据系统自动选择二进制文件）检测真连接，保存到proxy/available.txt
4. 从available.txt中挑选出最好的100个(可配置)，保存到proxy/best.txt
5. 日志输出到logs目录，7天滚动
补充：允许用户指定仓库列表，换言之，要搜索的仓库=用户指定的仓库列表+自动搜索的仓库列表。统一配置处理。
根据代理服务器地址分类，按国家，如Japan, Korea, USA, Taiwan, Russia等, 分别保存到不同的文件中。
同样检测旧数据，如果多次检测不可用，则移除。评估仓库提供的链接质量，如果某个仓库提供的链接质量很差（比如延迟高, 无效链接）则将其排除。
我需要不断累计优质可用的不同国家的代理，你有何建议？要补充什么？


由于我本地使用v2rayN，优先使用xray检测真连接，如果xray不可用，则使用singbox检测，如果singbox不可用，则使用mihomo检测。
需支持Linux，Windows，Mac。每个国家上限默认100（可配置修改）.如果对于一些地区的代理数量太少（<10），可以统一放入一个文件.
可以考虑分阶段，分频率执行，不用每次从头执行完再保存到文件，可不可以这样：每次处理500条，10分钟执行一次。不同步骤可以按不同频率执行。
请综合考虑以上因素，提出建议。update Solution and Save to the doc directory.


重构proxy，将获取代理数据的三种方式best，merge，pac中共同依赖的功能提取到一个独立模块，
设计并提供合理的api（如：分享链接和config的转换，tcp或真连接延迟），考虑未来增加其他获取代理方式时需要的功能。
将可执行的代理核心引擎放到bin目录，没有时从其官网下载最新版，设置Action，默认每3天检查更新。
补充完善以上内容，提供你的建议，设计专业的solution